[build]
target = "index.html"
dist = "dist"
# Pour build local en simulant le sous-répertoire :
# trunk build --release --public-url /thrustcode.dev/
# Pour dev local normal :
# trunk serve  (pas de public-url nécessaire)

[serve]
port = 3000
open = false

[watch]
ignore = ["dist", ".git", "node_modules", "app", "components/ui"]
